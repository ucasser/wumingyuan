# AGENTS.md

本文件是引文检索系统的调度规则。opencode 每次进入本项目时，必须优先读取并遵守本文件。

本项目所有路径均以项目根目录为基准，使用相对路径。本文件所在目录即为项目根目录。

将项目拷贝到任何电脑后，只需在项目根目录下启动 opencode 即可工作，无需修改任何路径。

---

## 〇、项目全流程总览

本项目从 PDF 原始文献到论文引文返回，共分三个阶段、八个步骤：

```
┌──────────────────────────────────────────────────────────────┐
│                       阶段一：导入                            │
│                    （用户操作，一次性）                        │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ① 用户准备 PDF 书籍 → 放入指定文件夹                         │
│                                                              │
│  ② 运行 pipeline/wumingyuan/                                 │
│     indexer.py: PDF → OCR 转 Markdown 全文                    │
│     store.py: 全文 → 双分块（知识块+引文块）→ LanceDB 向量库   │
│                                                              │
└──────────────────────────┬───────────────────────────────────┘
                           │ OCR 后的 MD 全文
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                      阶段二：加工                             │
│                  （本地运行，按需增量）                        │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ③ pipeline/splitbookmd/                                     │
│     splitbookmd_v12_stable_v2.py                              │
│     OCR MD → 识别目录 → 拆章拆节 → 清洗噪声 → 提取元数据       │
│     输出：按章节拆分、带页码标记的清洗版 MD                     │
│                                                              │
│  ④ build_small_to_big_v4.py                                  │
│     清洗版 MD → 切小块（~500字）+ 大块（~2200字）             │
│     → parents.jsonl + children.jsonl（全文关键词索引）         │
│                                                              │
│  ⑤ search_small_to_big_v3.py / batch_search_topics_v2.py      │
│     主题词 × JSONL 索引 → BM25 关键词匹配                     │
│     命中小块 → 自动回填大块上下文 → search_results.md          │
│                                                              │
│  ⑥ export_quote_cards_v3.py                                  │
│     search_results.md → 逐条解析 → 导出候选引文卡片 (.md)     │
│                                                              │
│  ⑦ add_obsidian_quote_tags_fixed.py                          │
│     候选卡片 → 添加 YAML 标签 + 主题链接 → Top3/Top10 最终卡片 │
│                                                              │
└──────────────────────────┬───────────────────────────────────┘
                           │ 2029 张 Top3 卡片
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                      阶段三：检索                             │
│               （LLM 按 AGENTS.md 规则执行）                    │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ⑧ LLM 读取本文件（AGENTS.md）第六节规则                      │
│     主题页 → 全量映射 → Top3 卡片 → 原始 MD 核验              │
│     每步执行 rg/grep 命令，逐层返回引文及来源                   │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 各阶段依赖

| 阶段 | 运行环境 | 核心依赖 | 产物 |
|---|---|---|---|
| 导入 | Python 3 | LanceDB, OCR 引擎 | OCR 的 MD 全文 |
| 加工 | Python 3 | 标准库为主 | Top3/Top10 引文卡片 |
| 检索 | 任何 LLM + ripgrep | rg/grep | 返回给用户的引文列表 |

### 文件目录对应

```
./pipeline/wumingyuan/     →  阶段一：PDF 导入
./pipeline/splitbookmd/    →  阶段二：MD 加工出卡片
./原文库_obsidianvault/    →  阶段三：卡片库（LLM 检索对象）
./原始md文档/              →  全文核验层
./AGENTS.md                →  阶段三检索规则
```

---

## 一、本地材料库使用规则

本项目的本地材料来源包括以下目录：

1. 原文库_obsidianvault/02_核心引文库T3
2. 原文库_obsidianvault/03_扩展引文库T10
3. 原始md文档/文档
4. 原始md文档/杂文
5. 原文库_obsidianvault/期刊库
6. 原文库_obsidianvault/主题页
7. 辅助索引/全量映射

材料使用优先级如下：

第一优先级：原文库_obsidianvault/主题页、辅助索引/全量映射  
用于理解主题组织、材料索引和相关文本分布。


第二优先级：原文库_obsidianvault/02_核心引文库T3  
用于优先寻找可以进入正文的直接引文。

第三优先级：原文库_obsidianvault/03_扩展引文库T10  
当核心引文库材料不足时，用于补充候选引文。

第四优先级：原始md文档/文档、原始md文档/杂文、原文库_obsidianvault/期刊库  
用于核验原文、补充上下文、确认出处、查找原书籍库相关内容。



不得把没有核验来源的文字当作直接引文使用。

---

## 二、引文库调用规则

本节专门规定 opencode 如何调用本地引文库。写作任务中凡涉及"找引文""匹配引文""核验引文""为某段补材料""根据材料写正文"等操作，必须遵守本节规则。

本项目的引文检索不是直接搜索整个材料库，而是按照固定路径逐层定位。标准路径为：

```text
主题页 → 全量映射 → 02_核心引文库T3 → 03_扩展引文库T10 → 原始 MD 核验
```

不得一开始直接检索整个 Obsidian vault。  
不得一开始直接检索原始 MD 文档。  
不得把辅助索引、全量映射或主题页本身当作论文直接引文来源。  
主题页和全量映射只负责定位材料，正式直接引文必须回到 Top3、Top10、原始 MD、期刊库或用户提供材料中确认。

---

### 1. 引文检索总顺序

opencode 查找引文时必须按以下顺序执行。

#### 检索工具说明

本项目依赖纯文本搜索，不依赖任何数据库或向量模型。首选 `rg`（ripgrep），如未安装则回退到系统自带 `grep`：

- 安装 `rg`：macOS 用 `brew install ripgrep`，Linux 用 `apt install ripgrep`
- 回退命令：将 `rg -n` 替换为 `grep -rn`，其余参数不变

所有检索路径均为本文件所在目录的相对路径，拷贝到任何电脑后无需修改。

#### 检索步骤

第一，先检索主题页：

```bash
rg -n "关键词1|关键词2|关键词3" "./原文库_obsidianvault/主题页"
```

第二，如果主题页中有相关主题，必须先阅读主题页中的以下内容：

```text
主题说明
相关概念
重点引文卡片
推荐检索词
写作用途
```

第三，如果主题页没有直接命中，或者用户给出的主题较冷门、较细碎，必须检索全量映射：

```bash
rg -n "关键词1|关键词2|关键词3" "./辅助索引/全量映射"
```

第四，根据主题页或全量映射中定位到的卡片名，打开 Top3 核心引文库：

```bash
rg -n "卡片名|关键词1|关键词2" "./原文库_obsidianvault/02_核心引文库T3"
```

第五，Top3 材料不足时，才检索 Top10 扩展引文库：

```bash
rg -n "关键词1|关键词2|关键词3" "./原文库_obsidianvault/03_扩展引文库T10"
```

第六，需要进一步核验卡片引文的原文、页码或上下文时，检索 OCR 转写后的全文 MD（无名园）：

```bash
rg -n "原文关键短语" "./原始md文档/无名园OCR/文档"
```

第七，核验项目原始全书 MD（博士项目核心书籍）：

```bash
rg -n "原文关键短语" "./原始md文档/文档"
```

第八，核验历史文献片段：

```bash
rg -n "原文关键短语" "./原始md文档/杂文"
```

#### 禁止事项

不得跳过主题页和全量映射，直接大范围读取 Top3、Top10 或原始 MD。  
不得一次性读取整个 Top3、整个 Top10 或整个原始 MD 目录。  
不得使用向量检索、全文索引或任何依赖外部服务的搜索方式替代上述逐层检索。

---

### 2. 主题页使用规则

主题页是引文检索的第一入口，不是直接引文来源。

主题页的作用是：

```text
确定研究主题
理解相关概念
定位重点引文卡片
获得推荐检索词
判断材料的写作用途
```

使用主题页时必须注意：

1. 如果用户给出的是宽泛题目，应先在主题页中寻找相近主题。
2. 如果主题页提供了推荐检索词，应优先使用这些检索词，而不是凭空扩展。
3. 如果主题页列出重点引文卡片，应根据卡片名回到 Top3 或 Top10 打开正式引文卡片。
4. 主题页中的说明文字可以帮助理解材料关系，但不能作为论文直接引文。
5. 如果主题页与 Top3、Top10 中的正式卡片不一致，应以正式引文卡片和原始 MD 核验结果为准。

---

### 3. 全量映射使用规则

`./辅助索引/全量映射` 是查表定位层，不是论文引文来源。它的作用是帮助 opencode 将用户问题转化为准确卡片入口。

使用规则如下：

1. 如果用户问题能在主题页中找到对应主题，应优先使用主题页。
2. 如果主题页没有命中，应查全量映射，寻找对应主题页、相近主题词、相关卡片、置信度和处理建议。
3. 如果全量映射显示某条主题词置信度较低，必须提醒用户该主题需要人工复核。
4. 从全量映射定位到卡片后，必须回到 Top3 或 Top10 打开正式引文卡片，不得引用映射表本身。
5. 全量映射适合精确定位，不适合作为正文材料来源。
6. 全量映射不得作为 Smart Connections 的索引对象。
7. 全量映射不得进入论文脚注。

---

### 4. 宽泛主题必须转化为精确检索词

用户给出的主题经常是宽泛表达。opencode 不得只用宽泛词检索后就下结论。

例如，用户说"苏联哲学"，应优先从主题页和全量映射中扩展为更精确的检索词，例如：

```text
德波林
机械论派
米丁
尤金
哲学党性
哲学战线
联共中央
斯大林
教条化
苏联新哲学
辩证唯物主义体系化
```

用户说"大众化"，应扩展为：

```text
新哲学大众化
艾思奇
大众哲学
胡绳
新哲学的人生观
陈唯实
哲学到大众中去
通俗宣传
读者对象
哲学普及
```

用户说"中国化"，应扩展为：

```text
马克思主义的中国化
中国化的马克思主义
实践论
矛盾论
毛泽东哲学思想
六届六中全会
中国作风
中国气派
中国特点
形式与内容
中国现实
```

扩展检索词时应优先依据主题页、全量映射和已有卡片中的关键词。不得脱离项目材料凭空制造一组看似相关但无法回到本地材料库验证的检索词。

---

### 5. Top3 与 Top10 的分工

Top3 是核心引文库。Top10 是扩展引文库。正式写作时必须 Top3 优先，Top10 备用。

Top3 的用途：

```text
优先寻找正式正文可用的直接引文
优先支撑核心论点
优先进入脚注占位
优先作为写作初稿的材料基础
```

Top10 的用途：

```text
补充 Top3 不足的材料
寻找背景材料
寻找对照材料
检查 Top3 是否遗漏重要上下文
辅助判断某一论点是否有更多材料支撑
```

允许检索 Top10 的情况：

```text
Top3 没有相关材料
Top3 只有少数材料且不足以支撑论证
需要更多例证、背景材料或对照材料
需要检查 Top3 是否遗漏重要上下文
需要为某一弱论点寻找更直接材料
```

使用 Top10 材料时必须更严格检查：

```text
命中词是否具体
上下文是否直接相关
是否只是弱相关召回
是否可以支撑拟写论点
是否需要回到原始 MD 核验
```

不得把 Top10 当作默认入口。不得在没有检索 Top3 的情况下直接使用 Top10。

---

### 6. 引文卡片阅读规则

每张引文卡片必须阅读以下内容后才能判断是否可用：

```text
卡片标题
查询词或主题词
命中结果数
分数
命中词
书名
标题路径
页码
检索命中的短段
回填原文上下文
```

不得只根据卡片标题、文件名、主题页链接或命中关键词判断材料可用。

优先使用以下材料：

```text
命中词具体
上下文围绕同一问题展开
包含明确判断或思想评价
来源书名清楚
标题路径清楚
页码可见
可以直接支撑本段判断
```

谨慎使用以下材料：

```text
只命中一个泛词
上下文只是背景叙述
页码为空
OCR 错字较多
材料只与主题弱相关
材料无法支撑本段核心判断
```

不得使用只命中"哲学""马克思主义""中国""研究"等泛词，且无法直接支持论点的材料。

---

### 7. 原始 MD 文档使用规则

`./原始md文档` 包含以下子目录：

- `./原始md文档/文档`：博士项目核心书籍的全书 MD
- `./原始md文档/无名园OCR/文档`：扩展文献的 OCR 转写 MD
- `./原始md文档/杂文`：历史文献片段

原始 MD 不是日常检索入口。只有以下情况才使用原始 MD：

```text
核验页码
核验 OCR
寻找卡片前后文
确认卡片引文是否断章取义
查找 Top3 或 Top10 未覆盖的重要材料
确认文献标题、章节标题或出处信息
```

回到原始 MD 后，不得擅自把不确定材料写成确定引文。  
如果无法确认页码或来源，必须标注"页码待核"或"出处待核"。  
如果原始 MD 与引文卡片存在差异，应提醒用户人工核验，并优先保留不确定标记。  
原始 MD 中新发现的材料如果要作为直接引文，必须在使用材料说明中注明"来自原始 MD，需人工复核"。

---

### 8. 引文材料输出格式

用户要求"找引文""列材料""查相关资料""给这一段补材料"时，默认输出 Markdown 表格：

```markdown
| 序号 | 主题 | 卡片名 | 来源 | 页码 | 可用原文 | 可用理由 | 风险提示 |
|---|---|---|---|---|---|---|---|
```

填写规则如下：

1. `主题`：写明材料服务于哪个论点或问题。
2. `卡片名`：写明来自哪个引文卡片。
3. `来源`：写明书名、文章名或期刊来源。
4. `页码`：有页码则写页码，无页码则写"页码待核"。
5. `可用原文`：必须尽量使用卡片中的原文表达，不得改写成自己的话冒充原文。
6. `可用理由`：说明该材料能支撑哪个判断。
7. `风险提示`：说明是否需要回到原始 MD 核验，是否存在 OCR 问题，是否只是背景材料。

如果材料只是背景材料，不适合作为核心引文，必须明确说明。  
如果需要回到原始 MD 核验，必须写"需回原始 MD 核验上下文"。  
如果没有找到合适材料，必须说明没有找到，不得杜撰。

---

### 9. 引文进入正文前的核验规则

任何直接引文进入正文前，必须至少满足以下条件：

1. 能明确说明它支撑哪个段落判断。
2. 能明确说明它来自 Top3、Top10、原始 MD、期刊库或用户提供材料。
3. 能保留原文表达，不改写直接引文。
4. 能标注来源信息。
5. 页码缺失时能标注"页码待核"。
6. 引文前有引用目的。
7. 引文后有正文分析。

如果材料只能提供背景，而不能支撑判断，应转化为叙述材料，不得作为核心直接引文。  
如果材料来源不清，应进入"使用材料说明"或"待核问题"，不得直接写入正文。

---

### 10. 常见引文任务流程

#### 任务一：查找某主题引文

```text
1. 将用户主题转化为关键词和相近主题词
2. 检索主题页
3. 阅读相关主题页
4. 如果主题页不足，检索全量映射
5. 根据主题页或全量映射定位 Top3 卡片
6. 打开相关 Top3 卡片
7. Top3 不足时检索 Top10
8. 必要时回到原始 MD 核验
9. 输出候选引文表格
10. 标注可用性和风险
```

#### 任务二：为某一目分配引文

```text
1. 先读取本目核心论题和段落群结构
2. 按段落判断点拆分引文需求
3. 对每个判断点检索主题页
4. 主题页不足时查全量映射
5. 回到 Top3 匹配候选引文
6. Top3 不足时查 Top10
7. 对每条候选引文说明支撑关系
8. 判断引文分布是否均衡
9. 输出"引文需求与匹配结果"
10. 等待用户确认后才能进入正文写作
```

#### 任务三：写一个论文段落

```text
1. 明确段落论点
2. 检索主题页
3. 必要时查全量映射
4. 读取相关 Top3 卡片
5. Top3 不足时读取少量 Top10 卡片
6. 选择最能支持论点的引文
7. 必要时回到原始 MD 核验页码和上下文
8. 写出符合项目写作规范的段落
9. 用 Markdown 脚注占位标注来源
10. 在使用材料说明中列出材料来源和风险
```

#### 任务四：核验某段论证

```text
1. 抽取段落中的核心判断
2. 查主题页、全量映射和 Top3
3. 判断现有引文是否支持该判断
4. 如果不支持，直接指出问题
5. 给出替代引文或改写方案
6. 不得为了迎合原段落而强行证明其成立
```

---

### 11. 引文库调用中的禁止事项

不得做以下事情：

```text
不得编造引文
不得编造页码
不得编造文献来源
不得把卡片标题当作原文
不得把主题页说明当作直接引文
不得把全量映射当作直接引文
不得只看文件名就写论文
不得只看命中词就判断材料可用
不得直接检索整个 vault
不得一开始直接读取整个 Top3 或 Top10
不得把 Top10 当作默认入口
不得把辅助索引当作论文引文来源
不得把原始 MD 当作日常入口
不得把网络资料伪装成本地引文
不得为了迎合用户而回避指出材料不足
```

如果用户的设想不利于准确写作或检索，必须直接指出问题，并给出更稳妥替代方案。

---

### 12. 引文库调用核心原则

本项目调用引文库的目标不是让材料越多越好，而是让写作时的引文更准确、更可核验、更省 token。

opencode 必须坚持：

```text
主题页优先
全量映射辅助定位
Top3 优先
Top10 备用
原始 MD 用于核验
网络资料只作转述
辅助索引不作引文来源
不编造
不乱搜
不大范围读取
不迎合错误方案
```
